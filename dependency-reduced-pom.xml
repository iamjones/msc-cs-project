<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.davidjones.msccsproject</groupId>
  <artifactId>msc-cs-project</artifactId>
  <name>msc-cs-project</name>
  <version>1.0-SNAPSHOT</version>
  <build>
    <finalName>msc-cs-project</finalName>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.6.1</version>
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven-shade-plugin.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                    <exclude>META-INF/LICENSE*</exclude>
                    <exclude>license/*</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>${exec-maven-plugin.version}</version>
        <executions>
          <execution>
            <goals>
              <goal>java</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <mainClass>rootmeansquareerror.RmseCalculator</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.hamcrest</groupId>
      <artifactId>hamcrest-all</artifactId>
      <version>1.3</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <properties>
    <stanfordnlp.version>3.8.0</stanfordnlp.version>
    <java.version>1.8</java.version>
    <hamcrest.version>1.3</hamcrest.version>
    <maven-shade-plugin.version>3.0.0</maven-shade-plugin.version>
    <guice.version>4.1.0</guice.version>
    <jacksoncore.version>2.8.9</jacksoncore.version>
    <guava.version>23.0</guava.version>
    <maven.compiler.target>1.8</maven.compiler.target>
    <boilerpipe.version>1.1.0</boilerpipe.version>
    <mrunit.version>1.1.0</mrunit.version>
    <exec-maven-plugin.version>1.6.0</exec-maven-plugin.version>
    <hbase.version>1.0.0</hbase.version>
    <log4j.version>2.8.2</log4j.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <hadoop.version>2.6.0</hadoop.version>
    <elasticsearch-hadoop.version>5.5.0</elasticsearch-hadoop.version>
    <maven.compiler.source>1.8</maven.compiler.source>
  </properties>
</project>

